import { DefaultTheme, StyledComponent } from 'styled-components';

declare module '@devseed-ui/form' {
  /**
   * Styled component for a `form`. Useful for style purposes.
   * Renders `<form>`
   */
  export const Form: StyledComponent<'form', DefaultTheme, {}, never>
}