import { DefaultTheme, StyledComponent } from 'styled-components';

declare module '@devseed-ui/form' {
  /**
   * Styled component for a `fieldset`. Useful for style purposes.
   * Renders `<fieldset>`
   */
  export const FormFieldset: StyledComponent<'fieldset', DefaultTheme, {}, never>

  /**
   * Styled component for a FormFieldsetHeader. Useful for style purposes.
   * Renders `<div>`
   */
  export const FormFieldsetHeader: StyledComponent<'div', DefaultTheme, {}, never>

  /**
   * Styled component for a FormLegend. Useful for style purposes.
   * Renders `<legend>`
   */
  export const FormLegend: StyledComponent<'legend', DefaultTheme, {}, never>

  /**
   * Styled component for a FormFieldsetBody. Useful for style purposes.
   * Renders `<div>`
   */
  export const FormFieldsetBody: StyledComponent<'div', DefaultTheme, {}, never>
}